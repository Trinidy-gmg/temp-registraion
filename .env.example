# Server-side only — set in Vercel/hosting env, never NEXT_PUBLIC_*
#
# Demo: RegistrationPage calls AdminSite, which forwards to HAMS with its own key.
# Use the public base URL of your AdminSite deployment (no trailing slash).
ADMINSITE_AUTH_BASE_URL=https://your-admin-site.example.com
